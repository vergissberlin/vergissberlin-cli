#!/usr/bin/env ruby
# frozen_string_literal: true

begin
  require "vergissberlin"
rescue LoadError
  $LOAD_PATH.unshift File.expand_path("../lib", __dir__)
  require "vergissberlin"
end

exit Vergissberlin::CLI.run(ARGV)
